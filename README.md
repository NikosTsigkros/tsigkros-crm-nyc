# Tsigkros CRM for NYC

This is a minimal Flask app so far

## Setup

```sh
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```sh
python app.py
```

Open http://127.0.0.1:5000
